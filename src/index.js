import React from 'react';
import ReactDOM from 'react-dom';
import Routues from './Routes';

ReactDOM.render(
  <React.StrictMode>
    <Routues />
  </React.StrictMode>,
  document.getElementById('root')
);

